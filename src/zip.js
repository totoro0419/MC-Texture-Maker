const textDecoder=new TextDecoder('utf-8',{fatal:false});const textEncoder=new TextEncoder();function u16(view,off){return view.getUint16(off,true)}function u32(view,off){return view.getUint32(off,true)}function put16(view,off,v){view.setUint16(off,v,true)}function put32(view,off,v){view.setUint32(off,v>>>0,true)}
export function crc32(bytes){let crc=0xffffffff;for(let i=0;i<bytes.length;i++){crc^=bytes[i];for(let k=0;k<8;k++)crc=(crc>>>1)^(0xedb88320&-(crc&1));}return(crc^0xffffffff)>>>0;}
async function inflateRaw(bytes){if(typeof DecompressionStream==='undefined')throw new Error('This browser does not support deflate decompression. Use a current Chromium/Edge/Chrome browser.');const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('deflate-raw'));return new Uint8Array(await new Response(stream).arrayBuffer());}
export async function readZip(input,onProgress=()=>{},include=null){const bytes=input instanceof Uint8Array?input:new Uint8Array(input);const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);let eocd=-1;const min=Math.max(0,bytes.length-0xffff-22);for(let i=bytes.length-22;i>=min;i--){if(u32(view,i)===0x06054b50){eocd=i;break;}}if(eocd<0)throw new Error('Not a supported ZIP: end-of-central-directory was not found.');const entryCount=u16(view,eocd+10);const centralOffset=u32(view,eocd+16);const entries=[];let p=centralOffset;for(let index=0;index<entryCount;index++){if(u32(view,p)!==0x02014b50)throw new Error('Invalid ZIP central directory.');const flags=u16(view,p+8),method=u16(view,p+10),compressedSize=u32(view,p+20),uncompressedSize=u32(view,p+24),nameLen=u16(view,p+28),extraLen=u16(view,p+30),commentLen=u16(view,p+32),localOffset=u32(view,p+42);if(compressedSize===0xffffffff||uncompressedSize===0xffffffff||localOffset===0xffffffff)throw new Error('ZIP64 archives are not supported yet.');const nameBytes=bytes.subarray(p+46,p+46+nameLen);const name=textDecoder.decode(nameBytes);if(name.startsWith('/')||name.split('/').some(part=>part==='..'))throw new Error(`Unsafe ZIP path rejected: ${name}`);const wanted=!include||include(name);if(wanted){if(u32(view,localOffset)!==0x04034b50)throw new Error(`Invalid local header for ${name}`);const localNameLen=u16(view,localOffset+26),localExtraLen=u16(view,localOffset+28),dataStart=localOffset+30+localNameLen+localExtraLen,compressed=bytes.subarray(dataStart,dataStart+compressedSize);let data;if(name.endsWith('/'))data=new Uint8Array();else if(method===0)data=compressed.slice();else if(method===8)data=await inflateRaw(compressed);else throw new Error(`Unsupported ZIP compression method ${method} for ${name}`);entries.push({id:crypto.randomUUID(),path:name,data,directory:name.endsWith('/'),flags,method,dirty:false});}p+=46+nameLen+extraLen+commentLen;onProgress((index+1)/entryCount,name);}return entries;}
function dosDateTime(date=new Date()){const year=Math.max(1980,date.getFullYear());const time=(date.getHours()<<11)|(date.getMinutes()<<5)|Math.floor(date.getSeconds()/2);const day=(year-1980)<<9|(date.getMonth()+1)<<5|date.getDate();return{time,day};}
export function writeZip(entries){const files=entries.filter(e=>!e.directory),locals=[],centrals=[];let offset=0;const{time,day}=dosDateTime();for(const entry of files){const name=textEncoder.encode(entry.path.replace(/^\/+/,''));const data=entry.data instanceof Uint8Array?entry.data:new Uint8Array(entry.data);const crc=crc32(data);const local=new Uint8Array(30+name.length+data.length),lv=new DataView(local.buffer);put32(lv,0,0x04034b50);put16(lv,4,20);put16(lv,6,0x0800);put16(lv,8,0);put16(lv,10,time);put16(lv,12,day);put32(lv,14,crc);put32(lv,18,data.length);put32(lv,22,data.length);put16(lv,26,name.length);put16(lv,28,0);local.set(name,30);local.set(data,30+name.length);locals.push(local);const central=new Uint8Array(46+name.length),cv=new DataView(central.buffer);put32(cv,0,0x02014b50);put16(cv,4,20);put16(cv,6,20);put16(cv,8,0x0800);put16(cv,10,0);put16(cv,12,time);put16(cv,14,day);put32(cv,16,crc);put32(cv,20,data.length);put32(cv,24,data.length);put16(cv,28,name.length);put16(cv,30,0);put16(cv,32,0);put16(cv,34,0);put16(cv,36,0);put32(cv,38,0);put32(cv,42,offset);central.set(name,46);centrals.push(central);offset+=local.length;}const centralOffset=offset,centralSize=centrals.reduce((n,b)=>n+b.length,0),eocd=new Uint8Array(22),ev=new DataView(eocd.buffer);put32(ev,0,0x06054b50);put16(ev,4,0);put16(ev,6,0);put16(ev,8,files.length);put16(ev,10,files.length);put32(ev,12,centralSize);put32(ev,16,centralOffset);put16(ev,20,0);return new Blob([...locals,...centrals,eocd],{type:'application/zip'});}
